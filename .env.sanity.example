# Sanity CMS Configuration
# Get these values from https://www.sanity.io/manage after creating a project

# Your Sanity Project ID
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id

# Dataset name (usually 'production')
NEXT_PUBLIC_SANITY_DATASET=production

# API Version (today's date in YYYY-MM-DD format)
NEXT_PUBLIC_SANITY_API_VERSION=2024-11-07

# Write token (get from sanity.io/manage -> API -> Add API Token)
# Only needed for write operations (creating/editing content)
SANITY_API_TOKEN=your-write-token-here
